---
// Callers pass sidebar block content via <slot />.
---
<aside>
  <pre id="flora" class="flora"></pre>
  <slot />
</aside>

<script>
  const ANIMS = [
    // 1. Spinning flower — moss
    {
      color: 'var(--moss)',
      ms: 200,
      frames: [
`   \\ | /
   ─(✶)─
   / | \\
    |||
   \\\\|//
~~~~~~~~~~`,
`   ─ | ─
   \\(✶)/
   ─ | ─
    |||
   \\\\|//
~~~~~~~~~~`,
`   / | \\
   ─(✶)─
   \\ | /
    |||
   \\\\|//
~~~~~~~~~~`,
`   ─ | ─
   /(✶)\\
   ─ | ─
    |||
   \\\\|//
~~~~~~~~~~`,
      ]
    },
    // 2. Blinking cat — brass
    {
      color: 'var(--accent)',
      ms: 500,
      frames: [
`  /\\_/\\
 ( ^.^ )
  > ~ <
    |
   / \\
~~~~~~~~~~`,
`  /\\_/\\
 ( -.^ )
  > ~ <
    |
   / \\
~~~~~~~~~~`,
`  /\\_/\\
 ( ^.- )
  > ~ <
    |
   / \\
~~~~~~~~~~`,
`  /\\_/\\
 ( -.- )
  > u <
    |
   / \\
~~~~~~~~~~`,
      ]
    },
    // 3. Bouncing ball — sky
    {
      color: 'var(--sky)',
      ms: 130,
      frames: [
`    ●




~~~~~~~~~~`,
`
    ●



~~~~~~~~~~`,
`

    ●


~~~~~~~~~~`,
`


    ●

~~~~~~~~~~`,
`



   (●)
~~~~~~~~~~`,
`


    ●

~~~~~~~~~~`,
`

    ●


~~~~~~~~~~`,
`
    ●



~~~~~~~~~~`,
      ]
    },
    // 4. Waving figure — terra
    {
      color: 'var(--terra)',
      ms: 250,
      frames: [
`   \\o/
    |
    |
   / \\

~~~~~~~~~~`,
`    o─
    |
    |
   / \\

~~~~~~~~~~`,
`   /o\\
    |
    |
   / \\

~~~~~~~~~~`,
`   ─o
    |
    |
   / \\

~~~~~~~~~~`,
      ]
    },
  ];

  const anim = ANIMS[Math.floor(Math.random() * ANIMS.length)];
  const el = document.getElementById('flora');
  el.style.color = anim.color;
  el.textContent = anim.frames[0];
  let frame = 1;
  setInterval(() => {
    el.textContent = anim.frames[frame % anim.frames.length];
    frame++;
  }, anim.ms);
</script>
